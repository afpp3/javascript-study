const images = document.querySelectorAll("img");

// images.forEach(function (item) {
//   console.log(item);
// });

// Usando arrow funcution =>
images.forEach((image) => {
  console.log(image);
});
